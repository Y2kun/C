#include <stdio.h>
#include <stdlib.h>

//probably not going to complete

const char map[3][50] = {"castle", "plains", "dune"};


struct Player {
    int pos;
    int level;
    int health;
    int strenght;
};

struct monster {
    int health;
    int strenght;
};

void attack(){

}

void init(){
    struct Player p;
    p.pos = 0;
}

int main() {
    if (0 == 0) {
        init();
        char posn = map[Player.pos];
        int aenemy = 0;
        int action = 0;
        if aenemy = 0
        prinf("Press 1 if you want to go back");
        prinf("Press 2 if you want to go forward");
        else
        prinf("Press 3 if you ant to attack");

        scanf("%i", &action);

    }
    return 0
}
